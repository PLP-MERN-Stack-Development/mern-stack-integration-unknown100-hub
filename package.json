{
  "name": "mern-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev --prefix server\" \"npm:dev --prefix client\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
